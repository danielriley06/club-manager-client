import * as React from "react";

interface IDefaultComponentProps {}

const DefaultComponent: React.FunctionComponent<
  IDefaultComponentProps
> = props => <h1>COMPONENT NOT LOADED</h1>;

export default DefaultComponent;
